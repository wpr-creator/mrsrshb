# Assets folder

Keep all future art/audio local so the site can run from GitHub Pages and offline after download.

Recommended structure:
- `ui/` buttons, panels, icons
- `backgrounds/` scene art
- `sprites/` characters, coins, rewards
- `audio/` short classroom-safe sounds

Use CC0/public-domain or school-owned assets only.
